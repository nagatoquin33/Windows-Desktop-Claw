{
  "name": "desktop-claw",
  "version": "0.0.1",
  "private": true,
  "description": "A lightweight desktop AI companion living as a floating ball on your macOS screen.",
  "license": "MIT",
  "packageManager": "pnpm@9.0.0",
  "scripts": {
    "dev": "pnpm --filter @desktop-claw/desktop run dev",
    "build": "pnpm --filter @desktop-claw/desktop run build",
    "typecheck": "pnpm -r run typecheck",
    "lint": "pnpm -r run lint"
  },
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=9.0.0"
  }
}
